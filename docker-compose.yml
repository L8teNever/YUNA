services:
  yuna:
    build: .
    ports:
      - "10.7.0.1:1456:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
    # Uncomment to mount host filesystem into container:
    # volumes:
    #   - /:/host:ro

networks:
  default:
    driver: bridge
    ipam:
      config:
        - subnet: 10.200.77.0/24
